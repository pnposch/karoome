services:
  karoo-downloader:
    build: .
    env_file: .env
    volumes:
      - ./data:/data:z      # persists token.json + downloaded.db
      - ./output:/output:z  # .fit files land here
    restart: "no"         # run-and-exit; triggered by host cron
